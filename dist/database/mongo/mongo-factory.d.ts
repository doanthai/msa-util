import { ConfigService } from '@nestjs/config';
import { DefaultEnvConfig } from '../../config/interfaces';
declare const _default: (configService: ConfigService<DefaultEnvConfig>) => {
    uri: string;
};
export default _default;

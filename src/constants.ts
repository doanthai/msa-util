export const CONFIG_OPTIONS = 'CONFIG_OPTIONS';

export enum LogType {
  info = 'log',
  error = 'error',
  warn = 'warn',
  debug = 'debug',
  verbose = 'verbose',
}

export enum Env {
  local = 'local',
  development = 'development',
  staging = 'staging',
  test = 'test',
  production = 'production',
}
